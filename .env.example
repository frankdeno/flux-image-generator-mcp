# Black Forest Lab API key (required)
BFL_API_KEY=your_api_key_here

# Image output directory (optional, defaults to './output')
OUTPUT_DIR=./output

# Logging level (optional, defaults to 'info')
# Possible values: debug, info, warn, error
LOG_LEVEL=info